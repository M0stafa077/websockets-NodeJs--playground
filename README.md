# websockets-NodeJs--playground

WebSockets in Node.js using Express - A very basic server &amp; client setup for real-time communication.

## Starting Guide

To start the application, simply run the following command:

```sh
docker compose up
```
